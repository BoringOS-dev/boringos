{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://boringos.dev/connectors/google/schemas/search_emails.input.json",
  "title": "Gmail search_emails input",
  "type": "object",
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string",
      "description": "Gmail search query (e.g. \"from:boss subject:invoice\")."
    },
    "maxResults": {
      "type": "number",
      "minimum": 1,
      "maximum": 500,
      "description": "Max results (default: 10)."
    }
  },
  "additionalProperties": false
}
