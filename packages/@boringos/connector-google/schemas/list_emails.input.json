{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://boringos.dev/connectors/google/schemas/list_emails.input.json",
  "title": "Gmail list_emails input",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "Gmail search query (e.g. \"from:boss is:unread\"). Optional."
    },
    "maxResults": {
      "type": "number",
      "minimum": 1,
      "maximum": 500,
      "description": "Max emails to return (default: 10)."
    }
  },
  "additionalProperties": false
}
